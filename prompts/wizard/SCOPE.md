# SCOPE

State: **SCOPE**

Goal: {{goal}}
Platforms: {{platforms}}
Missing fields: {{missing_fields}}

Prompt template:
- Ask for target platforms/channels as CSV list.
- Example variable usage: {{platforms}}
- Allow commands: `status` / `back` / `pause` / `resume`.
