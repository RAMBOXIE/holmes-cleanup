# AUTH

State: **AUTH**

Goal: {{goal}}
Platforms: {{platforms}}
Missing fields: {{missing_fields}}

Prompt template:
- Ask for authorization method (env token / oauth / manual).
- Mention missing fields: {{missing_fields}}
- Allow commands: `status` / `back` / `pause` / `resume`.
