# GOAL

State: **GOAL**

Goal: {{goal}}
Platforms: {{platforms}}
Missing fields: {{missing_fields}}

Prompt template:
- Ask: What is the primary cleanup/takedown goal?
- Example variable usage: {{goal}}
- Allow commands: `status` / `back` / `pause` / `resume`.
