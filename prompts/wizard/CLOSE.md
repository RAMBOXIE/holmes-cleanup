# CLOSE

State: **CLOSE**

Goal: {{goal}}
Platforms: {{platforms}}
Missing fields: {{missing_fields}}

Prompt template:
- Confirm wizard completion and next follow-up items.
- Commands remain available: `status` / `back` / `pause` / `resume`.
