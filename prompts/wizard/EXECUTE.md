# EXECUTE

State: **EXECUTE**

Goal: {{goal}}
Platforms: {{platforms}}
Missing fields: {{missing_fields}}

Prompt template:
- Ask final run command confirmation (run/execute/go).
- Mention missing fields: {{missing_fields}}
- Allow commands: `status` / `back` / `pause` / `resume`.
